<UserControl x:Class="ADHDPlanner.View.UserControls.Task"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ADHDPlanner.View.UserControls"
             mc:Ignorable="d" Height="150" Width="500"
             d:DesignWidth="800">
    <Grid>
        <Rectangle Fill="{Binding Color}" Panel.ZIndex="-1" Opacity="0.35"/>
        <TextBlock TextAlignment="Left" Margin="10, 10, 0, 0" FontSize="24" Text="{Binding Title}" />
    </Grid>
</UserControl>
