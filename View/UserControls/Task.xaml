<UserControl x:Class="ADHDPlanner.View.UserControls.Task"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ADHDPlanner.View.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid Background="{Binding Color}">
        <TextBlock TextAlignment="Left" VerticalAlignment="Top" Margin="0, 8, 0, 0" FontSize="25" FontFamily="Courier"
                   Text="{Binding Title}" Width="300" TextWrapping="Wrap" Padding="0, 0, 0, 40"/>

        <TextBlock Text="{Binding EstimatedTime}" TextAlignment="Right" VerticalAlignment="Bottom"
                   Margin="0, 5, 5, 5"/>
    </Grid>
</UserControl>
